export const metadata = {
  title: "Account Cockpit",
  description: "Manage your email campaigns, SMTP server configurations, and invoice billing details at Isha Software Solutions.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AccountLayout({ children }) {
  return children;
}
